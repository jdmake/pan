{% form_theme form '@Admin/form/bootstrap_3_layout.html.twig' %}

<div class="row">
    {{ form_errors(form) }}
    {{ form_start(form) }}
    {{ form_widget( form ) }}
    {{ form_end(form) }}
</div>
